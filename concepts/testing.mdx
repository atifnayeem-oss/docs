---
title: "Testing"
description: "A shared sandbox with ready-made merchants and callers. Test mode works before you configure anything, and one test card runs every payment."
---

Ringup's test mode is a shared sandbox that is live for every account from the first
command. There are no test keys to mint and no merchants to create before your first
working call. Every integration tests against this same sandbox.

## The sandbox test card

Every test payment runs on one card. When a sandbox call texts a pay link, or when you open a
sandbox Checkout page, enter this card:

| Field | Value |
|---|---|
| Card number | `4111 1111 1111 1111` |
| Expiry | any future date (for example `12/34`) |
| CVV | any 3 digits (for example `111`) |
| ZIP / postal code | any (for example `94103`) |

<Note>
  To test a declined payment, use `4000 0000 0000 0002`. It always declines, so you can
  exercise your failure path and the `checkout.failed` webhook.
</Note>

This card is sandbox-only. It never reaches a live payment processor and never moves real money.

## Fixtures

| Fixture | Value |
|---|---|
| Test merchant, payment optional under $50 | `tonys-pizza` |
| Test merchant, payment always required | `beacon-insurance` |
| Test caller with a saved card | `+14155550142` (Visa ending 5858) |

The test caller already has the sandbox card on file, so a session created for that number comes
back with one entry in `cards` and you can charge it straight away. A first-time (unrecognized)
caller gets an empty `cards`, is texted a link, and enters the sandbox card above.

A merchant id Ringup does not know is a `merchant_not_found` error, not a quiet no-op. A merchant
that exists but has connected no processor is the quiet case: the session returns
`payment_required: "none"` and payment is never mentioned on the call.

## Run a test payment

The general shape is the same on every platform:

<Steps>
  <Step title="Reach the sandbox">
    Point your agent or bridge at a sandbox merchant fixture and start a call, or create a
    Checkout Session against the sandbox with your Ringup API key.
  </Step>
  <Step title="Pay">
    As a recognized test caller (`+14155550142`), confirm the saved card. As a first-time
    caller, follow the texted link and enter the sandbox card above.
  </Step>
  <Step title="Read the result">
    A successful test charge returns `status: "succeeded"` with a `charge_id`, and the `order_id`
    the wrap-up step reads back to the caller. Your server receives a `checkout.succeeded` webhook.
  </Step>
</Steps>

Every integration page has a **Test it** section with the platform's specifics (which field
points at the sandbox, and what to expect on the call). On some platforms the Ringup dashboard
wires this up for you, binding a rehearsal number to a test copy of your agent.

## Reading test results

**The charge response is ground truth, not the assistant's words.** Only a `succeeded` charge
result with a `charge_id` proves a payment happened. An agent can narrate success it did not have;
the API result cannot.

**Text-chat smoke tests cannot prove voice.** Platform chat pipelines assemble handoffs
differently from voice pipelines. Validate with a voice call.

## Edge cases and gotchas

<AccordionGroup>
  <Accordion title="A declined card is a 402 with the standard error envelope" icon="triangle-exclamation">
    The decline card (`4000 0000 0000 0002`) returns `402`, carrying `decline_code`, `advice_code`
    and the sentence your agent can speak. Your ordinary `if (!response.ok)` check catches it. See
    [Errors](/api-reference/errors).
  </Accordion>
  <Accordion title="Web and dashboard test calls have no caller ID" icon="phone-slash">
    A call placed from a dashboard or web widget carries no real caller number, so recognition
    resolves to nothing. Pin the test caller (`+14155550142`) explicitly to exercise the
    recognized path. Real inbound phone calls resolve the caller natively.
  </Accordion>
  <Accordion title="The sandbox card never works live" icon="lock">
    `4111 1111 1111 1111` is sandbox-only. It never reaches a live payment processor and never
    moves real money, and a live key rejects it. Use a real card only under a `sk_live_` key.
  </Accordion>
</AccordionGroup>

## Next steps

<CardGroup cols={2}>
  <Card title="Pick your integration" icon="wrench" href="/concepts/how-it-works">
    Each platform page ends its setup with a Test it section using these fixtures.
  </Card>
  <Card title="Webhooks" icon="webhook" href="/concepts/webhooks">
    Watch the test-mode checkout.succeeded arrive at your endpoint.
  </Card>
</CardGroup>
